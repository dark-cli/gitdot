# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Commands and Apps to be executed at launch

# ============================================
# CUSTOM STARTUP APPS - Migrated from v2.3.13
# ============================================

$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts

$wallDIR=$HOME/Pictures/wallpapers
$lock = $scriptsDir/LockScreen.sh
$SwwwRandom = $UserScripts/WallpaperAutoChange.sh

# wallpaper stuff / More wallpaper options below (swww-daemon already started by configs/Startup_Apps.conf)
# exec-once = swww-daemon --format xrgb
#exec-once = $SwwwRandom $wallDIR # random wallpaper switcher every 30 minutes 

# Startup (already done by configs/Startup_Apps.conf)
# exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
# exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Polkit (already started by configs/Startup_Apps.conf)
# exec-once = $scriptsDir/Polkit.sh

# Core startup apps (waybar, nm-applet, swaync already started by configs/Startup_Apps.conf)
# exec-once = waybar &
# exec-once = nm-applet --indicator &
# exec-once = swaync &

# Custom apps
exec-once = firefox &
# exec-once = Discord & 
# exec-once = flatpak run com.spotify.Client

# Feishin - Navidrome music player
exec-once = ~/.local/share/feishin/Feishin.AppImage --no-sandbox &

#exec-once = ags &
#exec-once = blueman-applet & 
#exec-once = rog-control-center &

# Clipboard manager (already started by configs/Startup_Apps.conf)
# exec-once = wl-paste --type text --watch cliphist store 
# exec-once = wl-paste --type image --watch cliphist store

# Rainbow borders (already started by configs/Startup_Apps.conf)
# exec-once = $UserScripts/RainbowBorders.sh &

# Starting hypridle to start hyprlock (already started by configs/Startup_Apps.conf)
# exec-once = hypridle &

# Start pyprland daemon (for dropdown terminal)
exec-once = pypr &

# Sync tablet transform with monitor profile (CUSTOM)
exec-once = $scriptsDir/SyncTabletTransform.sh

# Here are list of features available but disabled by default
# exec-once = swww-daemon --format xrgb && swww img $HOME/Pictures/wallpapers/mecha-nostalgia.png  # persistent wallpaper

#gnome polkit for nixos
#exec-once = $scriptsDir/Polkit-NixOS.sh

# xdg-desktop-portal-hyprland (should be auto starting. However, you can force to start)
#exec-once = $scriptsDir/PortalHyprland.sh
